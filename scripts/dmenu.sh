#!/bin/bash
# Pipe text in here to return a user-selected line from that text.
# This looks prettier than regular dmenu

dmenu -i -l 15 -fn Monospace-18 -nb '#2c2c2e' -nf '#ff944d' -sb '#040404' -sf '#ff6600'
